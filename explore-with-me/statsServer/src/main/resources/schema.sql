DROP TABLE IF EXISTS ENDPOINTS_HIT CASCADE;

CREATE TABLE IF NOT EXISTS ENDPOINTS_HIT
(
    ID        BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    APP       VARCHAR                                 NOT NULL,
    URI       VARCHAR,
    IP        VARCHAR                                 NOT NULL,
    TIMESTAMP TIMESTAMP,
    CONSTRAINT ENDPOINTS_HIT_PK PRIMARY KEY (ID)
);